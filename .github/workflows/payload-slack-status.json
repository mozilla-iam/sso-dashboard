{
  "attachments": [
    {
      "color": "{{ env.STATUS_COLOR }}",
      "fields": [
        {
          "title": "{{ env.STATUS_TITLE }}",
          "short": true,
          "value": "[{{ env.RELEASE_NAME }}]\nStatus: {{ env.STATUS_VALUE }}"
        }
      ]
    }
  ]
}
