{
  "attachments": [
    {
      "color": "{{ env.STATUS_COLOR }}",
      "fields": [
        {
          "title": "[{{ env.RELEASE_NAME }}]\n{{ env.STATUS_TITLE }}",
          "short": true,
          "value": "Status: {{ env.STATUS_VALUE }}"
        }
      ]
    }
  ]
}
